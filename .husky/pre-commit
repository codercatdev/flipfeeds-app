#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

echo "🔍 Running pre-commit checks (biome check)..."
pnpm check

if [ $? -ne 0 ]; then
  echo "❌ Pre-commit checks failed. Run 'pnpm check:fix' to fix issues."
  exit 1
fi

echo "✅ Pre-commit checks passed!"
